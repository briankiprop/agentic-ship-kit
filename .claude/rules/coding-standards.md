# Coding Standards

- Prefer small functions with clear names.
- Keep public interfaces stable unless the plan explicitly changes them.
- Avoid broad refactors during feature work.
- Add comments for non-obvious decisions, not for obvious syntax.
- Prefer simple code over clever code.
- Keep generated files out of commits unless required.
- Update documentation for user-visible behavior.
