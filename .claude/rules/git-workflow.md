# Git Workflow

- Never work directly on `main` or `master`.
- Create a feature branch before implementation.
- Keep changes focused.
- Do not mix formatting-only changes with feature changes unless approved.
- Do not commit generated files unless required.
- Do not use `--no-verify` unless a human explicitly authorizes it.
- Do not merge automatically.
- Prepare PR title and body after review.
